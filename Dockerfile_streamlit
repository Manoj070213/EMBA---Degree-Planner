FROM python:3.10-slim
WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY degree_plan_app.py planner_core.py main.py ./

EXPOSE 8080
CMD ["sh", "-c", "streamlit run degree_plan_app.py --server.port=$PORT --server.address=0.0.0.0"]
